
// negative to positive it's called absolute 

// const myNumber = -5;
// const output = Math.abs(myNumber);  // always output positive number

const myNumber = 2.6356;
const output = Math.ceil(myNumber); //  dosomiker porer number upore uthiye integer banay felbe 
const output = Math.floor(myNumber); // dosomiker porer number niche namiye integer banay felbe 

const output = Math.round(myNumber); // dosomiker porer number round kore integer kore fele 
const output = Math.random();
const output = Math.random() * 6;
// if rounded or integer needed 
const rounded = Math.round(output); // floor use korle sobsomoy 6 er nicher man pabo 
console.log(rounded);

for (let i = 0; i <= 30; i++) {
    
    const output = Math.random() * 6;
    // if rounded or integer needed 
    const rounded = Math.round(output); // floor use korle sobsomoy 6 er nicher man pabo 
    console.log(rounded);

}
